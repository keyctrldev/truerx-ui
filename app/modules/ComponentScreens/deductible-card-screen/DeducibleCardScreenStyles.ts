import { StyleSheet } from 'react-native';
import { verticalScale } from '../../../theme';

export const styles = StyleSheet.create({
  deducibleCardContainerStyle: {
    marginVertical: verticalScale(10),
  },
});
