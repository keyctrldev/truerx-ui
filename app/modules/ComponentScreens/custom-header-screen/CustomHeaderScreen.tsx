import { View } from 'react-native';
import React from 'react';
import { CustomHeader } from '../../../components';

const CustomHeaderScreen = () => {
  return (
    <View>
      <CustomHeader heading="TESTING" />
    </View>
  );
};

export default CustomHeaderScreen;
